import { Component } from '@angular/core';

export class ServerStatComponent {
	constructor() { }
}

ServerStatComponent.annotations = [
	new Component({
		selector: 'server-stat',
		template: '<h1>Chitter</h1>',
	})
];